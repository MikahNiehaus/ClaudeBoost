{
  "description": "Centralized hook registry for all Claude Code instances",
  "note": "This file is read by install.py to auto-register hooks on any machine",
  "hooks": {
    "PreToolUse": [
      {
        "name": "proof-gate",
        "description": "Blocks edits without research proof file",
        "script": "$CLEAN_RAG_HOME/hooks/proof-gate.py",
        "applies_to": ["Edit", "Write", "MultiEdit"]
      },
      {
        "name": "agent-spawn-gate",
        "description": "Enforces RAG context + depth+breadth research pattern",
        "script": "$CLAUDEBOOST_HOME/clean-rag/hooks/agent-spawn-gate.py",
        "applies_to": ["Task"]
      }
    ],
    "UserPromptSubmit": [
      {
        "name": "rag-enforce",
        "description": "Injects research-first mandate every turn",
        "script": "$CLEAN_RAG_HOME/hooks/rag-enforce.py",
        "applies_to": ["all"]
      }
    ],
    "Stop": [
      {
        "name": "research-stop-gate",
        "description": "Blocks unresearched responses (inline prompt)",
        "type": "prompt",
        "applies_to": ["all"]
      }
    ],
    "PostToolUse": [
      {
        "name": "reindex-after-edit",
        "description": "Auto-reindex project after edits",
        "script": "$CLEAN_RAG_HOME/hooks/reindex-after-edit.py",
        "applies_to": ["Edit", "Write", "MultiEdit"]
      }
    ]
  },
  "setup_instructions": [
    "1. This registry is read by clean-rag/install.py",
    "2. When install.py runs, it registers all hooks in this registry",
    "3. All machines running the same CLAUDEBOOST_HOME will have identical hooks",
    "4. Hooks are stored in local settings.json but sourced from this registry"
  ]
}
