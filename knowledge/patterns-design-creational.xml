<knowledge-base name="patterns-design-creational" version="1.0">
<triggers>creational patterns, factory, builder, singleton, prototype, abstract factory, object creation</triggers>
<overview>Creational design patterns: object creation mechanisms that increase flexibility and reuse.</overview>
<source license="MIT">iluwatar/java-design-patterns (language-agnostic extracts)</source>

<pattern id="creational-01" slug="abstract-factory">
  <name>Abstract Factory</name>
  <category>Creational</category>
  <tags>Abstraction, Decoupling, Gang of Four, Instantiation, Polymorphism</tags>
  <also-known-as>* Kit</also-known-as>
  <intent>The Abstract Factory pattern in Java provides an interface for creating families of related or dependent objects without specifying their concrete classes, enhancing modularity and flexibility in software design.</intent>
  <explanation>A factory of factories; a factory that groups the individual but related/dependent factories together without specifying their concrete classes.</explanation>
  <applicability>Use the Abstract Factory pattern in Java when:

* The system should be independent of how its products are created, composed, and represented.
* You need to configure the system with one of multiple families of products.
* A family of related product objects must be used together, enforcing consistency.
* You want to provide a class library of products, exposing only their interfaces, not their implementations.
* The lifetime of dependencies is shorter than the consumer's lifetime.
* Dependencies need to be constructed using runtime values or parameters.
* You need to choose which product to use from a family at runtime.
* Adding new products or families should not require changes to existing code.</applicability>
  <tradeoffs>Benefits:

* Flexibility: Easily switch between product families without code modifications.

* Decoupling: Client code only interacts with abstract interfaces, promoting portability and maintainability.

* Reusability: Abstract factories and products facilitate component reuse across projects.

* Maintainability: Changes to individual product families are localized, simplifying updates.

Trade-offs:

* Complexity: Defining abstract interfaces and concrete factories adds initial overhead.

* Indirectness: Client code interacts with products indirectly through factories, potentially reducing transparency.</tradeoffs>
</pattern>

<pattern id="creational-02" slug="builder">
  <name>Builder</name>
  <category>Creational</category>
  <tags>Gang of Four, Instantiation, Object composition</tags>
  <intent>The Builder design pattern in Java, a fundamental creational pattern, allows for the step-by-step construction of complex objects. It separates the construction of a complex object from its representation so that the same construction process can create different representations.</intent>
  <explanation>Allows you to create different flavors of an object while avoiding constructor pollution. Useful when there could be several flavors of an object. Or when there are a lot of steps involved in creation of an object.</explanation>
  <applicability>Use the Builder pattern when

* The Builder pattern is ideal for Java applications requiring complex object creation.
* The algorithm for creating a complex object should be independent of the parts that make up the object and how they're assembled
* The construction process must allow different representations for the object that's constructed
* It's particularly useful when a product requires a lot of steps to be created and when these steps need to be executed in a specific sequence</applicability>
  <tradeoffs>Benefits:

* More control over the construction process compared to other creational patterns
* Supports constructing objects step-by-step, defer construction steps or run steps recursively
* Can construct objects that require a complex assembly of sub-objects. The final product is detached from the parts that make it up, as well as their assembly process
* Single Responsibility Principle. You can isolate complex construction code from the business logic of the product

Trade-offs:

* The overall complexity of the code can increase since the pattern requires creating multiple new classes
* May increase memory usage due to the necessity of creating multiple builder objects</tradeoffs>
</pattern>

<pattern id="creational-03" slug="dependency-injection">
  <name>Dependency Injection</name>
  <category>Creational</category>
  <tags>Decoupling, Dependency management, Inversion of control</tags>
  <also-known-as>* Inversion of Control (IoC)
* Dependency Inversion</also-known-as>
  <intent>To decouple the creation of object dependencies from their usage, allowing for more flexible and testable code.</intent>
  <explanation>Dependency Injection separates the creation of the client's dependencies from its own behavior.</explanation>
  <applicability>* When aiming to reduce the coupling between classes and increase the modularity of the application.
* In scenarios where the object creation process is complex or should be separated from the class usage.
* In applications requiring easier unit testing by allowing dependencies to be mocked or stubbed.
* Within frameworks or libraries that manage object lifecycles and dependencies, such as Spring or Jakarta EE (formerly Java EE).</applicability>
  <tradeoffs>Benefits:

* Enhances modularity and separation of concerns.
* Simplifies unit testing by allowing for easy mocking of dependencies.
* Increases flexibility and maintainability by promoting loose coupling.

Trade-offs:

* Can introduce complexity in the configuration, especially in large projects.
* Might increase the learning curve for developers unfamiliar with Dependency Injection patterns or frameworks.
* Requires careful management of object lifecycles and scopes.</tradeoffs>
</pattern>

<pattern id="creational-04" slug="factory">
  <name>Factory</name>
  <category>Creational</category>
  <tags>Abstraction, Encapsulation, Gang of Four, Instantiation, Polymorphism</tags>
  <intent>The Factory Design Pattern in Java is a creational pattern that defines an interface for creating an object but allows subclasses to alter the type of objects that will be created. This pattern promotes flexibility and scalability in your codebase.</intent>
  <explanation>Real-world example

&gt; Imagine a scenario in a bakery where different types of cakes are made using a Factory Design Pattern. The bakery's `CakeFactory` handles the creation process, allowing easy addition of new cake types without altering the core cake-making process. The `CakeFactory` can produce various types of cakes such as chocolate cake, vanilla cake, and strawberry cake. Instead of the bakery staff manually selecting ingredients and following specific recipes for each type of cake, they use the `CakeFactory` to handle the process. The customer simply requests a cake type, and the `CakeFactory` determines the appropriate ingredients and recipe to use, then creates the specific type of cake. This setup allows the bakery to easily add new cake types without modifying the core cake-making process, promoting flexibility and scalability.

Wikipedia says

&gt; Factory is an object for creating other objects – formally a factory is a function or method that returns objects of a varying prototype or class.

Sequence diagram</explanation>
  <applicability>* Use the Factory Design Pattern in Java when the class does not know beforehand the exact types and dependencies of the objects it needs to create.
* When a method returns one of several possible classes that share a common super class and wants to encapsulate the logic of which object to create.
* The pattern is commonly used when designing frameworks or libraries to give the best flexibility and isolation from concrete class types.</applicability>
  <tradeoffs>Benefits:

* Implementing the Factory Pattern in your Java application reduces coupling between the implementation and the classes it uses.
* Supports the Open/Closed Principle, as the system can introduce new types without changing existing code.

Trade-offs:

* The code can become more complicated due to the introduction of multiple additional classes.
* Overuse can make the code less readable if the underlying complexity of the object creation is low or unnecessary.</tradeoffs>
</pattern>

<pattern id="creational-05" slug="factory-kit">
  <name>Factory Kit</name>
  <category>Creational</category>
  <tags>Abstraction, Decoupling, Encapsulation, Generic, Instantiation, Object composition</tags>
  <also-known-as>* Object Kit
* Toolkit</also-known-as>
  <intent>The Factory Kit Pattern in Java is a powerful design pattern that helps in creating factories with separated builder and factory interfaces. This pattern is essential for managing complex object creation scenarios.</intent>
  <explanation>Factory kit is a configurable object builder, a factory to create factories.</explanation>
  <applicability>Use the Factory Kit Pattern when

* The factory class cannot anticipate the types of objects it must create, and a new instance of a custom builder is needed.
* A new instance of a custom builder is needed instead of a global one.
* The types of objects that the factory can build need to be defined outside the class.
* The builder and creator interfaces need to be separated.
* Game developments and other applications that have user customization.</applicability>
  <tradeoffs>Benefits:

* The Factory Kit Pattern in Java promotes loose coupling by eliminating the need to bind application-specific classes into the code.
* It simplifies the code by shifting the responsibility of instantiation to a factory object, making the development process more efficient.

Trade-offs:

* Can introduce complexity into the code by requiring additional classes and interfaces.
* Sometimes can lead to dependency issues if not properly managed.</tradeoffs>
</pattern>

<pattern id="creational-06" slug="factory-method">
  <name>Factory Method</name>
  <category>Creational</category>
  <tags>Encapsulation, Gang of Four, Instantiation, Object composition, Polymorphism</tags>
  <also-known-as>* Virtual Constructor</also-known-as>
  <intent>Define an interface for creating an object using the Factory Method Pattern, but let subclasses decide which class to instantiate. This creational design pattern lets a class defer instantiation to subclasses, enhancing code flexibility and maintenance.</intent>
  <explanation>It provides a way to delegate the instantiation logic to child classes.</explanation>
  <applicability>Use the Factory Method Pattern in Java when:

* Class cannot anticipate the class of objects it must create.
* Class wants its subclasses to specify the objects it creates.
* Classes delegate responsibility to one of several helper subclasses, and you want to localize the knowledge of which helper subclass is the delegate.</applicability>
  <tradeoffs>Benefits:

* The Factory Method Pattern provides hooks for subclasses, enhancing code flexibility and maintainability.
* Connects parallel class hierarchies.
* Eliminates the need to bind application-specific classes into the code. The code only deals with the product interface; hence it can work with any user-defined concrete product classes.

Trade-offs:

* Can complicate the code by requiring the addition of new subclasses to implement the extended factory methods.</tradeoffs>
</pattern>

<pattern id="creational-07" slug="monostate">
  <name>Monostate</name>
  <category>Creational</category>
  <tags>Encapsulation, Instantiation, Object composition, Persistence, Polymorphism</tags>
  <also-known-as>* Borg</also-known-as>
  <intent>The Monostate pattern is an alternative approach to achieving a singleton-like behavior in object-oriented design, ensuring a shared state in Java applications. It enforces a unique behavior where all instances of a class share the same state. Unlike the Singleton pattern, which restricts a class to a single instance, Monostate allows for multiple instances but ensures they all have a shared state.</intent>
  <explanation>Monostate allows multiple instances of a class to share the same state by channeling their state management through a common shared structure. This ensures consistent state across all instances while maintaining the facade of independent objects.</explanation>
  <applicability>Use the Monostate pattern in Java design patterns when

1. **Shared State Across Instances:** All instances of a class must share the same state. Changes in one instance should be reflected across all instances. Monostate offers more flexibility compared to the traditional Singleton pattern.

2. **Transparent Usage:** Unlike Singleton, which can be less transparent in its usage, Monostate allows for a more transparent way of sharing state across instances. Clients interact with instances of the Monostate class as if they were regular instances, unaware of the shared state.
...</applicability>
  <tradeoffs>Benefits:

* Provides a shared state without restricting the creation of multiple instances.
* Ensures consistency of data across instances.

Trade-offs:

* Can lead to hidden dependencies due to shared state, making the system harder to understand and debug.
* Reduces the flexibility to have instances with independent states.</tradeoffs>
</pattern>

<pattern id="creational-08" slug="multiton">
  <name>Multiton</name>
  <category>Creational</category>
  <tags>Decoupling, Instantiation, Object composition</tags>
  <also-known-as>* Registry of Singletons</also-known-as>
  <intent>The Multiton pattern in Java ensures a class has only unique named instances, providing a global point of access to them. Each named instance is accessed through a unique key, making it an essential part of Java design patterns.</intent>
  <explanation>The Multiton pattern is an extension of the Singleton pattern, offering a way to have a map of unique named instances instead of a single instance. This makes it a valuable Java design pattern for managing named instances efficiently.</explanation>
  <applicability>Use cases for the Multiton pattern in Java

* A class must have named instances, but only one instance for each unique key.
* Global access to these instances is necessary without requiring global variables.
* You want to manage shared resources categorized by key.</applicability>
  <tradeoffs>Benefits:

* Ensures controlled access to instances based on key.
* Reduces global state usage by encapsulating instance management within the pattern.

Trade-offs:

* Increased memory usage if not managed properly due to multiple instances.
* Potential issues with concurrency if not implemented with thread safety in mind.</tradeoffs>
</pattern>

<pattern id="creational-09" slug="object-pool">
  <name>Object Pool</name>
  <category>Creational</category>
  <tags>Game programming, Instantiation, Memory management, Performance, Resource management, Scalability</tags>
  <also-known-as>* Resource Pool</also-known-as>
  <intent>The Object Pool design pattern in Java manages a pool of reusable objects, optimizing memory management and application performance by recycling objects rather than creating and destroying them repeatedly.</intent>
  <explanation>Object Pool manages a set of instances instead of creating and destroying them on demand.</explanation>
  <applicability>Use the Object Pool pattern when

* You need to frequently create and destroy objects, leading to high resource allocation and deallocation costs.
* The objects are expensive to create and maintain (e.g., database connections, thread pools).
* A fixed number of objects need to be controlled, like in connection pooling.
* Object reuse can significantly improve system performance and resource management.</applicability>
  <tradeoffs>Benefits:

* Improved Performance: Reduces the overhead of object creation and garbage collection.
* Resource Management: Controls the number of instances, reducing resource contention and limiting resource usage.
* Scalability: Allows the application to handle more requests by reusing a fixed number of objects.

Trade-offs:

* Complexity: Adds complexity to the codebase, requiring careful management of the pool.
* Thread Safety: Requires careful handling of concurrent access to the pool, introducing potential synchronization issues.
* Initialization Cost: Initial creation of the pool can be resource-intensive.</tradeoffs>
</pattern>

<pattern id="creational-10" slug="prototype">
  <name>Prototype</name>
  <category>Creational</category>
  <tags>Gang of Four, Instantiation, Object composition, Polymorphism</tags>
  <also-known-as>* Clone</also-known-as>
  <intent>The Prototype pattern is used to specify the kinds of objects to create using a prototypical instance, and create new instances through object cloning.</intent>
  <explanation>Create an object based on an existing object through cloning.</explanation>
  <applicability>* When the classes to instantiate are specified at run-time, for example, by dynamic loading.
* To avoid building a class hierarchy of factories that parallels the class hierarchy of products.
* When instances of a class can have one of only a few different combinations of state. It may be more convenient to install a corresponding number of prototypes and clone them rather than instantiating the class manually, each time with the appropriate state.
* When object creation is expensive compared to cloning.
* When the concrete classes to instantiate are unknown until runtime.</applicability>
  <tradeoffs>Benefits:

Leveraging the Prototype pattern in Java applications

* Hides the complexities of instantiating new objects.
* Reduces the number of classes.
* Allows adding and removing objects at runtime.

Trade-offs:

* Requires implementing a cloning mechanism which might be complex.
* Deep cloning can be difficult to implement correctly, especially if the classes have complex object graphs with circular references.</tradeoffs>
</pattern>

<pattern id="creational-11" slug="registry">
  <name>Registry</name>
  <category>Creational</category>
  <tags>API design, Data access, Decoupling, Dependency management, Enterprise patterns, Instantiation</tags>
  <intent>Registry Design Pattern centralizes the creation and management of a global set of objects, providing a single point of access and ensuring controlled instantiation.</intent>
  <explanation>Registry is a well-known object that other objects can use to find common objects and services.</explanation>
  <applicability>* When you need to manage a global collection of objects.
* When you need to decouple the creation of objects from their usage.
* When you need to ensure a controlled lifecycle for objects, such as services or resources.
* When you want to avoid redundant creation of objects.</applicability>
  <tradeoffs>Benefits:

Key advantages of adopting the Registry pattern in Java

* Centralizes object management, making the application easier to maintain.
* Promotes reuse and sharing of objects, which can reduce memory footprint and initialization time.
* Facilitates decoupling between components.

Trade-offs:

* Can become a bottleneck if not implemented efficiently.
* May introduce a single point of failure if the registry is not designed to be fault-tolerant.
* Increases complexity in managing the lifecycle of objects.</tradeoffs>
</pattern>

<pattern id="creational-12" slug="singleton">
  <name>Singleton</name>
  <category>Creational</category>
  <tags>Gang of Four, Instantiation, Lazy initialization, Resource management</tags>
  <also-known-as>* Single Instance</also-known-as>
  <intent>Ensure a Java class only has one instance, and provide a global point of access to this singleton instance.</intent>
  <explanation>Ensures that only one object of a particular class is ever created.</explanation>
  <applicability>Use the Singleton pattern when

* There must be exactly one instance of a class, and it must be accessible to clients from a well-known access point
* When the sole instance should be extensible by subclassing, and clients should be able to use an extended instance without modifying their code</applicability>
  <tradeoffs>Benefits:

* Controlled access to the single instance.
* Reduced namespace pollution.
* Allows refinement of operations and representation.
* Permits a variable number of instances (more than one, if desired).
* More flexible than class operations.

Trade-offs:

* Difficult to test due to global state.
* Potentially more complex lifecycle management.
* Can introduce bottlenecks if used in a concurrent context without careful synchronization.</tradeoffs>
</pattern>

<pattern id="creational-13" slug="step-builder">
  <name>Step Builder</name>
  <category>Creational</category>
  <tags>Code simplification, Domain, Encapsulation, Extensibility, Instantiation, Interface</tags>
  <also-known-as>* Fluent Builder</also-known-as>
  <intent>The Step Builder pattern in Java is an advanced technique to create complex objects with clarity and flexibility. It is perfect for scenarios requiring meticulous step-by-step object construction.</intent>
  <explanation>The Step Builder pattern constructs complex objects incrementally through a series of defined steps, ensuring clarity and flexibility in the creation process.</explanation>
  <applicability>The Step Builder pattern in Java is used

* When constructing an object that requires multiple initialization steps.
* When object construction is complex and involves many parameters.
* When you want to provide a clear, readable, and maintainable way to construct an object in a step-by-step manner.</applicability>
  <tradeoffs>Benefits:

* Improves code readability and maintainability by providing a clear and concise way to construct objects.
* Enhances flexibility in creating objects as it allows variations in the construction process.
* Encourages immutability by separating the construction process from the object's representation.

Trade-offs:

* May result in more complex code due to the additional classes and interfaces required.
* Can lead to verbose code when many steps are involved.</tradeoffs>
</pattern>

<pattern id="creational-14" slug="type-object">
  <name>Type Object</name>
  <category>Creational</category>
  <tags>Abstraction, Code simplification, Data processing, Game programming, Extensibility, Instantiation, Object composition, Polymorphism</tags>
  <also-known-as>* Type Descriptor
* Type Safe Enumeration</also-known-as>
  <intent>Allow creation of flexible and extensible sets of related types.</intent>
  <explanation>Type Object pattern enables dynamic creation and management of flexible and extensible sets of related classes, ideal for Java developers seeking modularity without modifying existing codebase.</explanation>
  <applicability>This pattern can be used when:

* Use when you need to create an extensible set of related classes without modifying existing code.
* Ideal for scenarios where types and their behaviors need to be defined at runtime or in a flexible manner.
* Suitable for situations where the number of types is large and may change over time.
* The difference between the different 'types' of objects is the data, not the behaviour.</applicability>
  <tradeoffs>Benefits:

* Increases flexibility and extensibility of the code.
* Simplifies the addition of new types without modifying existing code.
* Enhances code readability by organizing related behaviors and properties.

Trade-offs:

* Can increase complexity if not managed properly.
* May lead to performance overhead due to dynamic type checking and handling.</tradeoffs>
</pattern>

</knowledge-base>