# Vector DB index (rebuilt from source files)
.rag-index/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.venv/
venv/

# Model cache (local model weights, 100MB-3GB per model)
.model-cache/
model_caches/

# Benchmark dataset files (downloaded from HuggingFace, regenerated by download scripts)
tests/data/*.npy
tests/data/*.jsonl
tests/data/csn_full_bench/
tests/data/beir/

# best_model_config.json is intentionally tracked (benchmark results)

