# ClaudeBoost — How It Works

This file described the old architecture: `mcp-rag-server/` on port 8612,
`agents/*.xml`, `knowledge/*.xml`, and a 24 agent roster. None of that exists
any more.

For the current architecture, read `docs/CLAUDEBOOST-REFERENCE.md`. It covers
the six agents, the one search server on port 8613, and the hooks. For the
search backend itself, read `clean-rag/CLAUDE.md`.
